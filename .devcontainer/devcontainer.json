{
  "name": "MariaDB + Python + Git",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "root",
  "workspaceFolder": "/workspace",
  "forwardPorts": [3306],
  "postStartCommand": "service mariadb start && mariadb -e \"CREATE DATABASE IF NOT EXISTS mydatabase;\" && if [ ! -d .git ]; then git init && git branch -M main; fi",
  "extensions": ["ms-python.python"]
}
