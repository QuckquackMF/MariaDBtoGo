{
  "name": "MariaDB DevContainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "vscode",
  "workspaceFolder": "/workspace",
  "forwardPorts": [3306],
  "postStartCommand": "sudo chown -R vscode:vscode /workspace && sudo service mariadb start && python3 .devcontainer/create_db.py",
  "extensions": [
    "ms-python.python"
  ]
}
