{
  "name": "MariaDB DevContainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "root",
  "workspaceFolder": "/workspace",
  "forwardPorts": [3306],
  "postStartCommand": "service mariadb start && mariadb -e \"CREATE DATABASE IF NOT EXISTS mydatabase;\" && pip3 install -r requirements.txt",
  "extensions": [
    "ms-python.python"
  ]
}
